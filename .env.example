# No environment variables are currently read by the code (no os.environ /
# getenv / load_dotenv calls exist in src/). This file is a placeholder for
# when that changes (e.g. an OPENAI_API_KEY if the OpenAI path gets wired up).
